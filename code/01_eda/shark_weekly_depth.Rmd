---
title: "Exploratory data analysis - shark depth"
author: "Ben Cresswell"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output: 
 html_document:
    code_download: true # set to false if you don't want a button to dl the Rmd
    code_folding: hide
    collapse: no
    df_print: paged
    fig_caption: yes
    fig_height: 6
    fig_width: 10
    highlight: textmate
    theme: spacelab
    toc: yes
    toc_float: yes
editor_options: 
  chunk_output_type: inline
output_dir: "docs"
documentclass: article
fontsize: 12pt
mainfont: Arial
mathfont: LiberationMono
monofont: DejaVu Sans Mono
classoption: a4paper
---


```{r}
knitr::opts_chunk$set(
	echo = TRUE,       # Set to F if you want to hide the code in the final doc (better to use "code folding: hide" in the YAML header and keep this on T)
	include = TRUE,    # Set to F if you want the code to run but no output in the final doc
	message = FALSE,   # 
	warning = FALSE,   #
	eval = TRUE       # If set to F will just print the text in between the chunks (and this setup chunk)
	)
```


```{r include=FALSE}
# Housekeeping
source("code/packages.R")
#rm(list=ls()) # Clear out environment if necessary
```

## Load data
```{r echo=FALSE, message=FALSE, warning=FALSE, fig.width=16, fig.height=6}
# Shark T/P data
load(file = "data/Rdata/shark_tp_data.Rdata")
# SST data
source(knitr::purl("code/00_wrangle/wrangling_SSTdata.Rmd", quiet = TRUE))
```


# Shark depths - weekly summary
```{r}
shark_weekly_depths <- 
shark_tp_data %>% 
  mutate(week_date = date(floor_date(detection_timestamp, "week"))) %>% 
  filter(!is.na(Depth)) %>% 
  group_by(week_date) %>% 
  summarise(weekly_max_depth = max(Depth),
            weekly_min_depth = min(Depth),
            weekly_mean_depth = mean(Depth),
            weekly_depth_sd = sd(Depth)) %>% 
  mutate(weekly_min_depth = if_else(weekly_min_depth <0, 0, weekly_min_depth))
shark_weekly_depths
```

## Plot weekly depths
```{r fig.width=16, fig.height=6}
shark_weekly_depth_plot <- 
shark_weekly_depths %>% 
  ggplot() +
  geom_line(aes(x = week_date, y = weekly_min_depth), colour = "blue") +
  geom_line(aes(x = week_date, y = weekly_mean_depth), colour = "grey30") +
  geom_smooth(aes(x = week_date, y = weekly_mean_depth), colour = "grey30", method = "gam", formula = y ~ s(x, bs = 'cs'), level = 0.95) +
  geom_line(aes(x = week_date, y = weekly_max_depth), colour = "red") +
  geom_smooth(aes(x = week_date, y = weekly_max_depth), colour = "red", method = "gam", formula = y ~ s(x, bs = 'cs'), level = 0.95) +
  scale_y_reverse() +
  theme_minimal() +
  xlab("Date") +
  ylab("Averaged weekly depths \n (m below sealevel)")
shark_weekly_depth_plot
```

